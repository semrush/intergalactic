### Run watch change css, html

`npm run watch`

**Change code in `src/`**

### Run server

`npm run serve`

**Open browser localhost:8080**

### Run build

`npm run build`

**Get compile code in lib/**
