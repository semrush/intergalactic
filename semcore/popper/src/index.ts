import { Popper, isInputTriggerTag } from '@semcore/base-components';
import type {
  Placement,
  PopperContext,
  PopperProps,
  PopperHandlers,
  PopperTriggerProps,
  PopperPopperProps,
  eventInteraction,
} from '@semcore/base-components';

export {
  /**
   * @deprecated Use Popper from `@semcore/base-components` instead. Package `@semcore/popper` will be removed in the next major release
   */
  Popper,
  /**
   * @deprecated Use isInputTriggerTag from `@semcore/base-components` instead. Package `@semcore/popper` will be removed in the next major release
   */
  isInputTriggerTag,
};

export type {
  /**
   * @deprecated Use Placement from `@semcore/base-components` instead. Package `@semcore/popper` will be removed in the next major release
   */
  Placement,
  /**
   * @deprecated Use PopperContext from `@semcore/base-components` instead. Package `@semcore/popper` will be removed in the next major release
   */
  PopperContext,
  /**
   * @deprecated Use PopperProps from `@semcore/base-components` instead. Package `@semcore/popper` will be removed in the next major release
   */
  PopperProps,
  /**
   * @deprecated Use PopperHandlers from `@semcore/base-components` instead. Package `@semcore/popper` will be removed in the next major release
   */
  PopperHandlers,
  /**
   * @deprecated Use PopperTriggerProps from `@semcore/base-components` instead. Package `@semcore/popper` will be removed in the next major release
   */
  PopperTriggerProps,
  /**
   * @deprecated Use PopperPopperProps from `@semcore/base-components` instead. Package `@semcore/popper` will be removed in the next major release
   */
  PopperPopperProps,
  /**
   * @deprecated Use eventInteraction from `@semcore/base-components` instead. Package `@semcore/popper` will be removed in the next major release
   */
  eventInteraction,
};

export default Popper;
