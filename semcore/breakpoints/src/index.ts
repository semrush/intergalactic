import {
  defaultBreakpoints,
  MediaList,
  DEFAULT_MEDIA,
  createBreakpoints,
} from '@semcore/base-components';

/**
 * @deprecated Use defaultBreakpoints from `@semcore/base-components` instead. Package `@semcore/breacpoints` will be removed in the next major release
 */
export default defaultBreakpoints;

export {
  /**
   * @deprecated Use MediaList from `@semcore/base-components` instead. Package `@semcore/breacpoints` will be removed in the next major release
   */
  MediaList,
  /**
   * @deprecated Use DEFAULT_MEDIA from `@semcore/base-components` instead. Package `@semcore/breacpoints` will be removed in the next major release
   */
  DEFAULT_MEDIA,
  /**
   * @deprecated Use createBreakpoints from `@semcore/base-components` instead. Package `@semcore/breacpoints` will be removed in the next major release
   */
  createBreakpoints,
};
