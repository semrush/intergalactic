import { Col, Row } from '@semcore/base-components';

export {
  /**
   * @deprecated Use Col from `@semcore/base-components` instead. Package `@semcore/grid` will be removed in the next major release
   */
  Col,
  /**
   * @deprecated Use Row from `@semcore/base-components` instead. Package `@semcore/grid` will be removed in the next major release
   */
  Row,
};
