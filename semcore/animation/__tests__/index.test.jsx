import { testing } from '@semcore/jest-preset-ui';
import { assert, expect, test, describe, beforeEach, vi } from 'vitest';

describe('Animation', () => {
  beforeEach(testing.cleanup);

  test('TODO', () => {
    expect(true).toEqual(true);
  });
});
