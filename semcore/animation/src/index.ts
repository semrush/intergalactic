import {
  Animation,
  animationContext,
  Transform,
  FadeInOut,
  Collapse,
  Slide,
  Scale,
} from '@semcore/base-components';
import type {
  FadeInOutProps,
  SlideProps,
  CollapseProps,
  AnimationProps,
} from '@semcore/base-components';

export {
  /**
   * @deprecated Use Animation from `@semcore/base-components` instead. Package `@semcore/animation` will be removed in the next major release
   */
  Animation,
  /**
   * @deprecated Use animationContext from `@semcore/base-components` instead. Package `@semcore/animation` will be removed in the next major release
   */
  animationContext,
  /**
   * @deprecated Use Transform from `@semcore/base-components` instead. Package `@semcore/animation` will be removed in the next major release
   */
  Transform,
  /**
   * @deprecated Use FadeInOut from `@semcore/base-components` instead. Package `@semcore/animation` will be removed in the next major release
   */
  FadeInOut,
  /**
   * @deprecated Use Collapse from `@semcore/base-components` instead. Package `@semcore/animation` will be removed in the next major release
   */
  Collapse,
  /**
   * @deprecated Use Scale from `@semcore/base-components` instead. Package `@semcore/animation` will be removed in the next major release
   */
  Scale,
  /**
   * @deprecated Use Slide from `@semcore/base-components` instead. Package `@semcore/animation` will be removed in the next major release
   */
  Slide,
  /**
   * @deprecated Use FadeInOutProps from `@semcore/base-components` instead. Package `@semcore/animation` will be removed in the next major release
   */
  type FadeInOutProps,
  /**
   * @deprecated Use SlideProps from `@semcore/base-components` instead. Package `@semcore/animation` will be removed in the next major release
   */
  type SlideProps,
  /**
   * @deprecated Use CollapseProps from `@semcore/base-components` instead. Package `@semcore/animation` will be removed in the next major release
   */
  type CollapseProps,
  /**
   * @deprecated Use AnimationProps from `@semcore/base-components` instead. Package `@semcore/animation` will be removed in the next major release
   */
  type AnimationProps,
};
