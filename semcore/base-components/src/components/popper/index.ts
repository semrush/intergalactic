import { Popper, isInputTriggerTag } from './Popper';
import type {
  Placement,
  PopperContext,
  PopperProps,
  PopperHandlers,
  PopperTriggerProps,
  PopperPopperProps,
  eventInteraction,
  NSPopper,
} from './Popper.type';

export { Popper, isInputTriggerTag };

export type {
  Placement,
  PopperContext,
  PopperProps,
  PopperHandlers,
  PopperTriggerProps,
  PopperPopperProps,
  eventInteraction,
  NSPopper,
};
