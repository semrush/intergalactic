import { Popper, isInputTriggerTag } from './Popper';
import type {
  Placement,
  PopperContext,
  PopperProps,
  PopperHandlers,
  PopperTriggerProps,
  PopperPopperProps,
  eventInteraction,
} from './Popper.types';

export { Popper, isInputTriggerTag };

export type {
  Placement,
  PopperContext,
  PopperProps,
  PopperHandlers,
  PopperTriggerProps,
  PopperPopperProps,
  eventInteraction,
};
