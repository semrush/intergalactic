import { NeighborLocation, useNeighborLocationDetect } from '@semcore/base-components';
import type { NeighborLocationProps, NeighborItemProps } from '@semcore/base-components';

export {
  /**
   * @deprecated Use NeighborLocation from `@semcore/base-components` instead. Package `@semcore/neighbor-location` will be removed in the next major release
   */
  NeighborLocation,
  /**
   * @deprecated Use useNeighborLocationDetect from `@semcore/base-components` instead. Package `@semcore/neighbor-location` will be removed in the next major release
   */
  useNeighborLocationDetect,
};
export type {
  /**
   * @deprecated Use Box from `@semcore/base-components` instead. Package `@semcore/neighbor-location` will be removed in the next major release
   */
  NeighborLocationProps,
  /**
   * @deprecated Use Box from `@semcore/base-components` instead. Package `@semcore/neighbor-location` will be removed in the next major release
   */
  NeighborItemProps,
};

/**
 * @deprecated Use NeighborLocation from `@semcore/base-components` instead. Package `@semcore/neighbor-location` will be removed in the next major release
 */
export default NeighborLocation;
