import { Flex } from '@semcore/base-components';
import Divider from '@semcore/divider';
import React from 'react';

const Demo = () => {
  return (
    <Flex w={200} h={20}>
      <Divider />
    </Flex>
  );
};

export default Demo;
