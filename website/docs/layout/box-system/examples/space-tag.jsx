import React from 'react';
import { Box } from '@semcore/ui/flex-box';
import Button from '@semcore/ui/button';

const Demo = () => (
  <div>
    <Box tag={Button} mr={2}>
      Button
    </Box>
    <Box tag={Button} mr={2}>
      Button
    </Box>
    <Box tag={Button} mr={2}>
      Button
    </Box>
    <Box tag={Button} mr={2}>
      Button
    </Box>
  </div>
);

export default Demo;
